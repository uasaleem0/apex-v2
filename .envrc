# APEX Directory Auto-Banner
# Automatically displays APEX banner when entering this directory

# Check if we haven't shown the banner in this session
if [ -z "$APEX_BANNER_SHOWN" ]; then
    source ./apex-startup.sh
    export APEX_BANNER_SHOWN=1
fi